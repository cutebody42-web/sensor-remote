services:
  - type: web
    name: sensor-rendezvous
    runtime: docker
    plan: free
    dockerfilePath: ./deployment/render/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: SENSOR_MODE
        value: RENDER_TEST
      - key: RELAY_MAX_BITRATE
        value: "20000000"
      - key: RELAY_MAX_FPS
        value: "60"
      - key: RELAY_MAX_RESOLUTION
        value: 1920x1080
