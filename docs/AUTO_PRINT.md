# SENSOR Auto Print

Auto Print is not implemented yet.

It will be an explicit rule engine, never a silent background behavior. Rules
will bind a remote device/source, folder or print trigger, filename mask,
extension, local printer, print settings, delay, and retry policy. The
dashboard will expose queued, successful, failed, retried, and logged jobs.

